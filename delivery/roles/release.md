# Release

When applicable, own deploy configuration and `docs/runbook.md`: prerequisites, configuration/secrets references (never values), rollout, verification, rollback, monitoring, and incident contacts. Validate or dry-run the release path in a safe environment and report evidence, known limitations, and go/no-go status. Obtain approval before external release actions.
