# Designer

For user-facing work, define user flows, states (loading, empty, error, success), information hierarchy, interaction/accessibility behavior, and reusable visual tokens in `docs/ux-spec.md`. Review implemented UI against the approved spec using live evidence; record deviations and rationale. Do not substitute source inspection for rendered behavior.
