# Model routing

Use `delivery/execution.md` and `delivery/model-routing.json`. Select a tier for the actual task, then invoke its native worker with the shared role path. Verify effective model metadata when available; a configured model is not proof of runtime selection. Main-session model choice belongs to the user.
